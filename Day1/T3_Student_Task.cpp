/*
 * =====================================================================
 * 💻 STUDENT TASK: ENCAPSULATION (Hiding Data)
 * =====================================================================
 * 
 * YOUR MISSION:
 * 1. Create a `class SmartPhone { ... };`.
 * 2. In the `private:` section, add an integer called `batteryLevel`.
 *    (This stops outside hackers from changing the battery to 1000000%).
 * 3. In the `public:` section, add a method called `playGame()`.
 *    - It should decrease batteryLevel by 15.
 *    - It should print: "Playing game... Battery is now [batteryLevel]%"
 * 4. In the `public:` section, add a method called `chargePhone()`.
 *    - It should set the batteryLevel back to 100.
 *    - It should print: "Phone charged to 100%"
 * 5. In main(), create a phone, charge it, and play a game!
 */

// Grab the input/output library for printing
#include <iostream>
// Use the standard namespace
using namespace std;

// 1. Write your SmartPhone class here:
class SmartPhone {
// Add your private data here...


// Add your public methods here...


}; // End class with semicolon!

// Program starts here
int main() {
    
    // 2. Create your SmartPhone object here
    
    
    // 3. Charge the phone and play a game!
    

    // Exit successfully
    return 0;
}
